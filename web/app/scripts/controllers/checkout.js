'use strict';

/**
 * @ngdoc function
 * @name webappApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the webappApp
 */
angular.module('webappApp')
    .controller('CheckoutCtrl', ['$scope', '$rootScope', function ($scope, $rootScope) {
        $rootScope.sliderAndContent = true;

    }]);

