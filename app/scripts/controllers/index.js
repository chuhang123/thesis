'use strict';

/**
 * @ngdoc function
 * @name thesisApp.controller:IndexCtrl
 * @description
 * # IndexCtrl
 * Controller of the thesisApp
 */
angular.module('thesisApp')
  .controller('IndexCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
